(() => {

    enum audioLevel {
        min = 1,
        medium = 5,
        max = 10
    }

    enum estatus {
        V = 'Vigente',
        A = 'Abstención',
        C = 'Cancelado'
    }

    enum enumeracion {
        a = 10,
        b,
        c = 9,
        d
    }

    // let currentAudio = audioLevel.max
    // let miEstatus = estatus.C
    let gero = enumeracion.d

    //console.log(currentAudio)

    console.log(gero)
})()